# dc-api-webhook — Reference Kubernetes Manifests

These manifests are **historical reference only**.
The live deployment is managed by Terraform in the
`wso2-datacenter-project` repo, not by applying these files directly.
Do not `kubectl apply` them manually — Terraform will overwrite the result.

See `CLAUDE.md` section "Deployment is Terraform-driven" for the canonical workflow.
